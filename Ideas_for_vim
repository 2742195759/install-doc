一. 手工 tag 标记工具
  对于阅读代码而言，很多时候，寻找tag不准确。而且没有很好的 tag 代码。所以我们可能对某一个代码库进行手动寻找。找到合适的之后。希望可以存储下来当前的 -> 之后的跳转，以防止多次跳转。所以希望
  可以有一个tag的工具，可以半自动添加 tag ，并且将当前的 tag 优先级放到所有的 tag 之前。
  例如： main.py :
            ...
            func()
            ...
        
        utils.py : 
            def func(): #line 32
                ...
       
   那么我们希望可以在找到之后，将 func 这个 tag 存储下来，然后以后搜索 func 的时候，可以优先跳出这个  utils.py:32 出来。可以设置为自定义的文件。
